git pull origin main

git add .

git commit -m "Ficha 03: Exercício 1 concluído"

git push

Comando,O que faz na prática?
git status,(Opcional) Use sempre para ver se há ficheiros a vermelho (não guardados).
git log